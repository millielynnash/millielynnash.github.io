// layout.tsx in the same folder as page.tsx

export const metadata = {
  title: '...',
  description: '...',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
